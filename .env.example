# ============================================
# 监控系统环境变量配置模板
# ============================================
# 使用说明：
# 1. 复制此文件为 .env.development 或 .env.production
# 2. 根据实际环境填写对应的配置值
# 3. 不要将包含真实密码的 .env 文件提交到代码仓库
# 4. 此文件位于项目根目录，所有子项目（packages/* 和 demos/*）都会自动读取

# ============================================
# 域名配置
# ============================================
# 开发环境域名
# 开发环境下的应用访问地址
DEV_DOMAIN=http://localhost:5173

# 生产环境域名
# 生产环境下的应用访问地址
PROD_DOMAIN=https://your-production-domain.com

# API 服务域名
# 后端 API 服务的域名地址
API_DOMAIN=https://api.your-domain.com

# 监控服务域名
# 监控数据上报服务的域名地址
MONITOR_DOMAIN=https://monitor.your-domain.com

# ============================================
# 服务器连接配置
# ============================================
# 监控数据上报地址（DSN - Data Source Name）
# 格式：https://your-domain.com/api/monitor
MONITOR_DSN=https://example.com/api/monitor

# 项目标识（API Key）
# 用于标识不同的项目或应用
MONITOR_API_KEY=your-api-key-here

# 用户ID（可选）
# 用于标识用户，如果不设置则使用默认值
MONITOR_USER_ID=

# ============================================
# 服务器认证配置
# ============================================
# 服务器连接账号
# 用于连接监控服务器的用户名
MONITOR_SERVER_USERNAME=admin

# 服务器连接密码
# 用于连接监控服务器的密码（请妥善保管）
MONITOR_SERVER_PASSWORD=your-password-here

# 服务器主机地址
# 监控服务器的主机地址或IP
MONITOR_SERVER_HOST=localhost

# 服务器端口
# 监控服务器的端口号
MONITOR_SERVER_PORT=8080

# 数据库连接配置（如果需要）
# 数据库服务器地址
DB_HOST=localhost

# 数据库端口
DB_PORT=5432

# 数据库名称
DB_NAME=monitor_db

# 数据库用户名
DB_USERNAME=db_user

# 数据库密码
DB_PASSWORD=db_password

# Redis 连接配置（如果需要）
# Redis 服务器地址
REDIS_HOST=localhost

# Redis 端口
REDIS_PORT=6379

# Redis 密码（如果有）
REDIS_PASSWORD=

# ============================================
# 其他配置项（可选）
# ============================================
# 录屏时间间隔（秒）
# 默认值：10
MONITOR_RECORD_SCREEN_TIME=10

# 用户行为最大记录数
# 默认值：50
MONITOR_MAX_BREADCRUMBS=50

# XHR 请求超时阈值（毫秒）
# 超过此时间将上报为慢请求，默认值：5000
MONITOR_XHR_TIMEOUT_THRESHOLD=5000

# 环境标识
# 可选值：development, production, test
NODE_ENV=development

# ============================================
# Vite 客户端环境变量（可选）
# ============================================
# 注意：在 Vite 中，只有以 VITE_ 开头的环境变量才会暴露给客户端代码
# 敏感信息（如密码）不应该使用 VITE_ 前缀

# 客户端可访问的监控数据上报地址
# VITE_MONITOR_DSN=https://example.com/api/monitor

# 客户端可访问的项目标识
# VITE_MONITOR_API_KEY=your-api-key-here

# 客户端可访问的用户ID
# VITE_MONITOR_USER_ID=

# 客户端可访问的 API 域名
# VITE_API_DOMAIN=https://api.your-domain.com

# 客户端可访问的监控服务域名
# VITE_MONITOR_DOMAIN=https://monitor.your-domain.com
